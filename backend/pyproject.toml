[project]
name = "ctlchat-backend"
version = "0.1.0"
description = "RAG chatbot backend built with FastAPI, ChromaDB, and Claude"
requires-python = ">=3.11,<3.14"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
